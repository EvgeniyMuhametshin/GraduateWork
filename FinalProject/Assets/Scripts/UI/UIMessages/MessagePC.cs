using UnityEngine;

//Написать отдельную логику для компьютера 
public class MessagePC : MonoBehaviour
{
    //входим в тригер 
    //включаем текст
    //если(у игрока есть объект ключ)
    //выводим текст (ОБЪЕКТ РАБОТАЕТ)
    //иначе
    //выводим текст (ОБЪЕКТ НЕ РАБОТАЕТ)

    //выходим из тригера
    //выключаем текст
}
